apiVersion: v1
kind: Service
metadata:
  name: notification
  namespace: financeiro
  labels:
    app: notification
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: notification
